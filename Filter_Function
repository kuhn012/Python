my_list = [1,2,3,4,5]

def multiply_by2(item):
    return item * 2

def check_if_odd(item):
    return item % 2 != 0

print(list(filter(check_if_odd, my_list)))
print(my_list)

##Filter function inputs a function and a list or iterables
##It evaluates for true expressions, so function must return boolean
##If True, the iterable is kept, if not discarded
##Doesn't effect outside variables
